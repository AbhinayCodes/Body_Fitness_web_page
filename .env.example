# Optional for direct browser-to-API calls. The production frontend uses the
# server-side BACKEND_API_URL rewrite when it is configured.
NEXT_PUBLIC_API_URL="http://127.0.0.1:8000/api/v1"
BACKEND_API_URL="http://127.0.0.1:8000"
